# Use rust image
FROM rust:1.33

# Set working directory of rust app
WORKDIR /usr/src/web_dev

# Copy current directory, into docker
COPY . /usr/src/web_dev

#Create volume for release
VOLUME ./target:/usr/src/web_dev/target

# Make port 8080 available
EXPOSE 8000:8000

# Run the app
RUN cargo build

CMD ["bash"]
